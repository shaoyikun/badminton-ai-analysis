# INTERACTION-SPEC

## 1. 设计原则
- 移动端优先，默认竖屏单列布局
- 每个页面只承载一个核心任务
- 降低首次使用门槛
- 减少无效上传
- 报告结构先结论后细节
- 清晰告诉用户下一步做什么

## 2. 核心页面
- 首页
- 拍摄规范页
- 上传页
- 分析中页
- 报告页
- 历史记录页
- 复测对比页
- 错误状态页

说明：
- `首次引导页`、`上传确认页`、`分析完成页` 已从独立页面集合移除

## 3. 页面流转
- 主流程：首页 → 拍摄规范 → 上传 → 分析中 → 报告
- 辅助流程：首页 / 报告 → 历史记录 → 复测对比
- 异常流程：上传 / 分析中 → 错误状态页

## 4. 关键页面要求
### 首页
- 快速建立产品认知
- 当前正式 runtime 已支持 `clear + smash`，首页应展示动作选择入口
- 清晰的“开始分析当前动作”入口
- 历史记录可作为次入口

### 上传页
- 上传页应展示当前动作选择器，并让后续上传检查、确认文案和历史范围与当前动作保持一致
- 视频上传
- 基础信息预览
- 上传约束提示
- 上传前就绪检查
- 页内确认卡
- 点击确认后直接进入分析流程

### 分析中页
- 必须展示当前动作与阶段进度
- 必须展示当前任务摘要和分步骤实时说明
- 成功后直接进入报告页
- 失败后直接进入错误页

### 报告页
信息层级：
1. 动作名称 + 一句话结论 + 辅助总分 + 当前复测状态
2. 这次先练这一件事：核心问题、影响、下次练习怎么做、下次复测怎么看
3. 最近一次复测结论（有基线时）
4. 标准动作关键帧对比
5. 紧凑维度分数与其余需要继续看的问题
6. 历史与当前基线入口
7. 再次上传 / 历史记录入口

### 历史记录页
信息层级：
1. 已有同动作历史样本数量
2. 为什么要继续复测
3. 历史样本列表
4. 历史详情
5. 在历史详情中明确当前基线状态
6. 在历史详情中一键设为当前基线并查看对比

### 复测对比页
信息层级：
1. 本次整体变化结论
2. 当前样本 vs 当前基线
3. 最明显的改善项
4. 最该防止继续回落的点
5. 下次复测只先盯什么

### 错误状态页
- 必须说明错误原因
- 必须解释为什么会失败
- 必须给出重拍或重传建议
- 必须有返回拍摄规范或重新上传入口
- 返回上传页后必须能看到上次失败摘要，但需要重新选择文件

## 5. 用户可见状态映射
- `created`：待上传
- `uploaded` + 预处理阶段：上传中 / 校验中
- `processing`：分析中
- `completed`：已完成
- `failed`：失败

## 6. 错误映射约束
- `upload_failed` / `invalid_duration`：优先回上传
- `multi_person_detected` / `body_not_detected` / `poor_lighting_or_occlusion` / `insufficient_pose_coverage`（严重 coverage deficit）：优先回拍摄规范
- `invalid_camera_angle` 与边界型 `insufficient_pose_coverage`：优先完成为 `low_confidence` 报告，不默认进入失败页
- `preprocess_failed` / `pose_failed`：允许重新上传，也允许回看拍摄规范

## 7. 原型对应
当前移动端原型在：`prototype/`

## 8. 相关主文档
- `docs/design/INTERACTION-DESIGN.md`
- `docs/design/WIREFRAMES.md`
